# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS junk
.vscode/
.idea/
*.swp
.DS_Store

# Ad-hoc / debug dumps (keep the canonical full sweep radar_acc_did_sweep.txt + .log)
dumps/*_did_sweep_*.txt
# tools/dump.sh raw candump captures (transient)
dumps/dump-*.log
dumps/sniff/
*.tmp

# Unattended auto-drive-logger output + runtime state (tools/auto_drive_logger.py)
tmp/

# Transient drive/hunt logger scratch if run with default out-dir (keep curated *_did_sweep.* only)
projects/*/dumps/hunt_*.csv
projects/*/dumps/radar_acc_drive_*.csv
