# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS junk
.vscode/
.idea/
*.swp
.DS_Store

*.tmp

# ALL machine-written data lands under tmp/ (captures, sweeps, per-project logs, runtime state).
# Nothing a tool writes is ever tracked; promote anything worth keeping into projects/<x>/findings/.
tmp/
